#include<stdio.h> 
int main()
{ int x ;
  scanf("%d",&x);
    if (x % 2 == 0)
         { printf("%d einai peritos",x);
         }
    else 
         {printf("%d einai artios ",x);
         }
         return 0 ;
}         
